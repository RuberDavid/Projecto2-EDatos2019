#include <stdio.h>

int main()
{
	bool buy=1;
	const double capital= 100000;
	double val;
	long long int count=0;
	double expected_val;

	scanf("%lf", &val);
	if( val <= capital )


	buy= 
	//miéntras haya datos por leer
	while( scanf("%f",val) == 1 )
	{
		//si el valor es mayor que la esperanza
		if( val > expected_val )
		{
			//vende
		//si no
		}else
		{
			//comprar
		}
		//guarda el valor  en una acumulada
		expected_val= expected_val + val/count;//TODO: mal :C
		
		//obten la nueva esperanza
	}
}
